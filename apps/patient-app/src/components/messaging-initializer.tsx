'use client';

/**
 * MessagingInitializer (V2 - Firebase Free Replacement)
 * Browser-based push notifications via FCM have been disabled 
 * to remove the Firebase client SDK dependency. 
 * Notifications are now handled exclusively via WhatsApp/SMS by the backend.
 */
export function MessagingInitializer() {
    // Firebase Messaging disabled in frontend
    return null;
}
